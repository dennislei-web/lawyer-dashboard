# Supabase（必要）
# 從 https://supabase.com/dashboard 進 zpbkeyhxyykbvownrngf 專案
# → Project Settings → API → service_role secret（繞過 RLS，只給後端腳本用，絕對不能進 git）
SUPABASE_URL=https://zpbkeyhxyykbvownrngf.supabase.co
SUPABASE_SERVICE_KEY=eyJxxxxxxxxx

# Anthropic API（律師備忘單 LLM 歸因會用到）
# 從 https://console.anthropic.com/ 拿 key
# 沒有 key 也能跑備忘單（會走 inline 模式由 Claude 直接分析），但 50+ 筆案件強烈建議用 API 模式
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxx

# 批次建 Auth 使用者時的預設密碼（scripts/create_auth_users.py 會用）
DEFAULT_PASSWORD=ChangeMe123!

# CRM 抓資料用（如果你不會碰 CRM 同步腳本可以留空）
CRM_USERNAME=
CRM_PASSWORD=

# Voyage AI（向量檢索，目前實驗用，可留空）
VOYAGE_API_KEY=
